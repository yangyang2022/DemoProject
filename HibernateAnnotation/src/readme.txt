
1: 尽量不使用双向关联
2: 大项目不使用关联,增加冗余字段(id) 替代外键,如果需要几个表级联查询,一般使用StudentDto(传输)
    缺点:
    2.1 在修改时,需要修改所有的冗余,
    2.2 不再使用HQL ,全部使用sql查询,如果涉及缓存,根据情况加入缓存,不使用hibernate缓存
    (hibernate中的sql查询不进session ... 效率也很高 ... )
